{
	"name": "quiz-rag",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "concurrently \"cd packages/frontend && bun dev\" \"cd packages/backend && bun run --env-file=../../.env src/index.ts\"",
		"format": "bunx @biomejs/biome format --write ."
	},
	"devDependencies": {
		"@types/bun": "latest",
		"concurrently": "^9.2.1"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"workspaces": [
		"packages/*"
	]
}
